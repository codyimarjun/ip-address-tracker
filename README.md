# ip-address-tracker
A responsive IP address tracker app built with Vanilla JS, Leaflet.js, and the IP Geolocation API. Enter any IP or domain to get its location, timezone, and ISP displayed on an interactive map. Loads user's IP by default.
